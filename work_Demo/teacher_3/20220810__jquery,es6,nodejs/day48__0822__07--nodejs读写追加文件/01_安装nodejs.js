
   /* 
        2022年8月22日09:03:43
        nodejs是可以将js运行在服务端的
        语法和js一样，
        JavaScript组成：ECMAScript(es5,es6)，DOM（文档对象模型），BOM（浏览器对象模型）---运行在浏览器上。
       
        nodejs是一门服务端语言，，所有的前端框架都是在nodejs上集成的。
        nodejs如果学好可以自己独立开发一个项目，全栈。
        nodejs类似于java,php,C++,c#
        
        nodejs特点：
        1.可以运行js的服务端语言。
        2.非阻塞I/O  (I/O是输入/输出，)
        3.里面存在大量的异步编程。


        同步任务：上一句代码执行完毕，下一句代码才开始执行。
        异步任务：上一句代码并没有执行完，下一句代码就开始执行。

        nodejs组成：没有BOM和DOM，支持js
        组成：ECMAScript(es5,es5....都支持)，file（文件操作系统），network（必须有网络才可以访问）
            os(操作系统，windows,linux) database（数据库）---运行在服务器上

        windows比较简单，但是没有linus稳定安全。
        数据库分为MySQL，nosql。关系型数据库和非关系型数据库。

        安装node环境,  nodejs中文网--下载  ---msi  ---运行--next--i accept--change(look in可以安装在其他盘)--next --online documentation shortouts在线配置环境变量--next---对号--install--弹出一个窗口---windows powershell
        检测是否安装好--window--cmd--node -v  

        卸载nodejs  


        为什么js可以运行在node环境中？
       浏览器基于v8引擎， node也是基于谷歌的v8引擎。

        当然，js也可以运行在java环境中。
 */