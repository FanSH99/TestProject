/*  8
    层级选择器

    语法                        用法                        说明
    parent()                $('li').parent();           
    children(selector)      $('ul').children('li')      
    find(selector)          $('ul').find('li')          
    siblings(selector)      $(.first).siblings('li')    
    nextAll([expr])         $('.first').nextAll()       
    preAll([expr])          $('.first').prevAll()       
    hasClass(class)         $('div').hasClass('current')
    eq(index)               $('li').eq(2)              
*/
/*  5
    jQuery 筛选选择器：
    
    语法          示例            描述
    :first      $('li:first')   
    :last       $('li:last')    
    :eq(index)  $('li:eq(2)')   
    :odd        $('li:odd')     
    :even       $('li:even')    
*/
/*  8
    语法结构： $("选择器"),和CSS选择器一样用法，但是加引号  

    用法                  名词                  
    $("#id")           
    $("*")             
    $(".class")        
    $("div")           
    $("div,p,li")      
    $("li.current")    
    $("ul>li")         
    $("ul li")         
*/
/*   29
    动态资源：
    静态资源：
    B/S 架构：
    C/S 架构：
    会话：
    jQuery 的顶级对象是：
    jquery 转化为 DOM：
    DOM 转化为 jquery：
    隐式迭代：
    链式编程的原理：
    CSS操作(读写)：
    类操作(添加，删除，切换):
    获取索引号：
    show(参数1,参数2,参数3)每个参数的意义：
    animate(参数1,参数2,参数3,参数4)每个参数的意义：
    hover(参数1,参数2)参数意义:
    hover(参数1)参数意义:
    系统自带属性的操作：
    自定义属性的操作：
    parents()作用：
    获取指定祖先元素：
    获取执行子元素：
    $.each()用法：
    创建元素：
    内部追加元素：
    插件的使用步骤及核心：
    拷贝对象的函数：
    解除事件绑定的关键字：
    绑定事件的关键字：
*/
